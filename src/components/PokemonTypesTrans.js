/*import PokemonTypes from "./PokemonTypes";

function PokemonTypesTransition({ThePokemon}) {
    return (
        <ul className="pokemon-types">
            {ThePokemon.types.map(({Type},index) => 
                <div key={index}>
                    <PokemonTypes 
                        type={Type.type.name}
                    />
                </div>
            )}
        </ul>
    )
}

export default PokemonTypesTransition*/